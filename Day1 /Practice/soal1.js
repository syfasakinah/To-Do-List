// define variable
const length = 5;
const width = 3;

const area = rectangleArea(length, width);
console.log("Area:", area);


// Function
function rectangleArea(length, width) {
    return length * width;
}
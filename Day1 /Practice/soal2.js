// define variable
const length = 5;
const width = 3;

const perimeter = calculatePerimeter(length, width);
// const parimeter2 = 2 * (length + width);

// Function
function calculatePerimeter(length, width) {
    return 2 * (length + width);
}

// untuk nampilin hasil/output
console.log("Perimeter:", perimeter);
// console.log("Perimeter tanpa function:", parimeter2);